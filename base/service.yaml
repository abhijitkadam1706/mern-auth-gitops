apiVersion: v1
kind: Service
metadata:
  name: mern-auth
  labels:
    app: mern-auth
spec:
  selector:
    app: mern-auth
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 9191
  type: ClusterIP
